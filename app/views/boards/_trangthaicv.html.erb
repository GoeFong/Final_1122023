<% CongviecUser::TRANGTHAI_CVCB.each do |trangthaicol| %>
    <% if trangthaicol[:gia_tri] == donvi.status %>
        <!-- Danh sách 1 -->
        <div class="w-1/3">
            <div class="bg-gray-200 p-2 rounded-md mb-4">
                <h2 class="text-lg font-semibold mb-2 <%= trangthaicol[:color] %>"><%= trangthaicol[:label] %></h2>
                <!-- Thẻ công việc -->
                <div class="bg-white p-2 rounded-md mb-2">
                    <div class="text-sm font-semibold">Công việc 1</div>
                    <div class="text-gray-600 text-xs mt-1">Mô tả công việc...</div>
                </div>
                <!-- Thêm thẻ công việc mới -->
                <div class="bg-gray-300 p-2 rounded-md text-gray-600 text-sm cursor-pointer hover:bg-gray-400">Thêm thẻ công việc mới</div>
            </div>
        </div>
    <% end %>
<% end %>